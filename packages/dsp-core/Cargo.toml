[package]
name = "dsp-core"
version = "0.1.0"
edition = "2021"
description = "Optional Rust/Wasm DSP backend for Push Tutor synth engine"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = true
